"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Animal');
const Fixture = require('../../fixtures/AnimalFixture');

describe('models:Animal', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
